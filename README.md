# my_project

A minimal Python project scaffold with a sample package and tests.

Quickstart

Create a virtual environment, install dev deps, and run tests:

```bash
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
pip install -e .
pytest
```

Package layout

- `src/` - package source
- `tests/` - unit tests
